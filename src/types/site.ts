export type NavLink = {
  label: string;
  href: string;
};

export type InterestLink = {
  label: string;
  href: string;
};

export type ParishGroup = {
  name: string;
  shortDescription: string;
  description: string;
  image: string;
};
